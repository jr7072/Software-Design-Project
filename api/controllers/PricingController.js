//just create a class for now, nothing too bad

class pricing{
    constructor(numGallons, getAddress, dateOfDelivery){
    this.numGallons = numGallons
    this.getAddress = getAddress
    this.dateOfDelivery = dateOfDelivery
    this.clear()
    }
}